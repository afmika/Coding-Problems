function toCoord(row, col) {
    return `<${row}, ${col}>`;
}

function remenabilaPaikady(sarintany, toeranaHiaingana, tananaHotafihana) {
    let mat = sarintany;
    let dep = toeranaHiaingana;
    let targ = tananaHotafihana;
    let result = [ toCoord(dep[0], dep[1]) ] ;
}


function go(str, map, d, a) {
	let dr = [1, -1];
	let path = '';
	dr.forEach( dir => {
		if ( map[ d[0] + dir ][] == 1) {
			
		}
		if ( map[ ]
	});
	return false;
}


remenabilaPaikady([
	[[1, 0, 0],
	 [1,1,1]
	 [0,0,1]],
	 
	 [0,0],
	 [2, 2]
])